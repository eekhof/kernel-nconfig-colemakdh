--- nconf.c.default.6.16.5	2025-09-18 21:08:28.623174550 +0200
+++ nconf.c	2025-09-19 00:40:45.497841384 +0200
@@ -1110,11 +1110,11 @@
 				break;
 			switch (res) {
 			case KEY_DOWN:
-			case 'j':
+			case 'e':
 				menu_driver(curses_menu, REQ_DOWN_ITEM);
 				break;
 			case KEY_UP:
-			case 'k':
+			case 'i':
 				menu_driver(curses_menu, REQ_UP_ITEM);
 				break;
 			case KEY_NPAGE:
@@ -1134,7 +1134,7 @@
 				show_help((struct menu *) item_data());
 				break;
 			}
-			if (res == 10 || res == 27 ||
+			if (res == 10 || res == 27 || res == 111 || res == 110 ||
 				res == 32 || res == 'n' || res == 'y' ||
 				res == KEY_LEFT || res == KEY_RIGHT ||
 				res == 'm')
@@ -1144,7 +1144,7 @@
 
 		refresh_all_windows(main_window);
 		/* if ESC or left*/
-		if (res == 27 || (menu != &rootmenu && res == KEY_LEFT))
+		if (res == 27 || (menu != &rootmenu && res == KEY_LEFT) || (menu != &rootmenu && res == 110))
 			break;
 
 		/* remember location in the menu */
@@ -1167,6 +1167,7 @@
 				conf(submenu);
 			break;
 		case KEY_RIGHT:
+		case 111: /* O WAS PRESSED */
 		case 10: /* ENTER WAS PRESSED */
 			switch (item_tag()) {
 			case 'm':
@@ -1182,7 +1183,7 @@
 				else if (submenu->prompt &&
 					 submenu->prompt->type == P_MENU)
 					conf(submenu);
-				else if (res == 10)
+				else if (res == 10) // Do not add rightclick here this to prevent accidental change of activation when navigating
 					sym_toggle_tristate_value(sym);
 				break;
 			case 's':
@@ -1198,10 +1199,10 @@
 					btn_dialog(main_window, setmod_text, 0);
 			}
 			break;
-		case 'n':
-			if (item_is_tag('t'))
-				sym_set_tristate_value(sym, no);
-			break;
+		// case 'n': // Disable to prevent accidental deselection with colemakdh navigation controls, use SPACE instead to toggle state
+		// 	if (item_is_tag('t'))
+		// 		sym_set_tristate_value(sym, no);
+		// 	break;
 		case 'm':
 			if (item_is_tag('t'))
 				sym_set_tristate_value(sym, mod);
@@ -1317,14 +1318,14 @@
 				show_help((struct menu *) item_data());
 				break;
 			}
-			if (res == 10 || res == 27 || res == ' ' ||
+			if (res == 10 || res == 27 || res == ' ' || res == 111 ||
 					res == KEY_LEFT){
 				break;
 			}
 			refresh_all_windows(main_window);
 		}
 		/* if ESC or left */
-		if (res == 27 || res == KEY_LEFT)
+		if (res == 27 || res == KEY_LEFT || res == 110)
 			break;
 
 		child = item_data();
@@ -1332,6 +1333,7 @@
 			continue;
 		switch (res) {
 		case ' ':
+		case 111:
 		case  10:
 		case KEY_RIGHT:
 			choice_set_value(menu, child->sym);
